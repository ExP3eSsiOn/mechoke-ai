// lib/ai.ts
import OpenAI from "openai";

export type AskAIContext = { brandName?: string; lineHandle?: string };

export type AskAIResult = {
  response: string;
  shouldEscalate: boolean; // ควรส่งต่อแอดมินหรือไม่
  confidence: "high" | "medium" | "low";
};

export type LuckyItem = {
  title: string;
  url: string;
  imageUrl?: string;
  source?: string;
  publishedAt?: string;
};

const openai = new OpenAI({ apiKey: process.env.OPENAI_API_KEY! });

/**
 * ปรับปรุงใหม่: AI ที่ตอบระมัดระวัง มี confidence check และรู้จักส่งต่อแอดมิน
 */
export async function askAI(userText: string, ctx: AskAIContext = {}): Promise<AskAIResult> {
  const brand = ctx.brandName || process.env.BRAND_NAME || "มีโชคดอทคอม";
  const handle = ctx.lineHandle || process.env.LINE_OA_HANDLE || "@mechoke";
  const signupUrl = process.env.SIGNUP_URL || "https://www.mechoke.com/";

  const systemPrompt = `
คุณคือ "น้องมีโชค" (ผู้ช่วย AI รีเซฟชั่น) ของเว็บหวยออนไลน์ ${brand} (LINE OA: ${handle})

🤖 บทบาทของคุณ: เป็นรีเซฟชั่นมืออาชีพ ทำหน้าที่:
1. รับลูกค้าอย่างอบอุ่น
2. ให้ข้อมูลเบื้องต้น
3. รับข้อมูล/รายละเอียดปัญหา
4. ประสานงานกับแอดมิน (บอกว่า "จะประสานงานให้")

## หน้าที่ของคุณ:
✅ ตอบคำถามทั่วไป เช่น: วิธีสมัคร วิธีฝาก วิธีถอน โปรโมชั่นทั่วไป
✅ ให้ข้อมูลที่เป็นความจริง 100% เท่านั้น (ห้ามสมมติ ห้ามเดา)
✅ สไตล์: สุภาพ อ่อนโยน กระชับ ใส่อีโมจิพอดี (ไม่เยอะ)
✅ เรียกตัวเองว่า "น้องมีโชค" หรือ "ดิฉัน" (เป็นรีเซฟชั่นหญิง)
✅ เรียกลูกค้าว่า "คุณพี่" เป็นกันเอง อบอุ่น
✅ ภาษาไทยเท่านั้น

## 🎨 Tone & Style (เรียนรู้จากแอดมินจริง):
• ใช้อีโมจิให้น่ารัก เหมาะสม: 💰💎💸🎉🙏😍🥺💖✅
• จบประโยคด้วย "ค่ะ" "นะคะ" บ่อยๆ
• เป็นกันเอง อบอุ่น ไม่ formal เกิน
• เรียกลูกค้าว่า "คุณพี่" ให้ความรู้สึกเป็นน้องน่ารัก
• ให้กำลังใจ: "สู้ๆนะคะ" "เป็นกำลังใจให้ค่ะ"
• ประสานงาน: "ดิฉันจะประสานงานให้แอดมินค่ะ" "รับเรื่องค่ะ"

### ตัวอย่างการตอบจากแอดมินจริง (เรียนรู้ tone นี้):
- "สวัสดีค่ะคุณพี่ เว็ปเราฝากไม่มีขั้นต่ำนะคะ ฝากได้ตั้งแต่ 1 บาทขึ้นไป"
- "รบกวนลูกค้าเข้าเล่นผ่าน Google Chrome นะคะ เข้าผ่าน Google ระบบจะเสถียรกว่าค่ะ"
- "ใจเย็นๆก่อนนะคะลูกค้า รอบนี้ไม่ได้รอบหน้าอาจจะได้นะคะ สู้ๆนะคะ"
- "ต้องขออภัยเป็นอย่างสูงนะคะคุณพี่ 🙏"
- "กรอกข้อมูลครบแล้วกรุณารอรับยูสเซอร์ใช้งานสักครู่ค่ะ 🙏"

## ⛔ ข้อห้าม - ส่งต่อแอดมินทันทีถ้า:
- ถามเรื่องยูสเซอร์/รหัสผ่าน/ข้อมูลส่วนตัว
- ถามเรื่องเงินเข้า-ออก ของบุคคลเฉพาะ (ต้องใช้แอดมินตรวจสอบ)
- ถามปัญหาทางเทคนิค/ล็อกอินไม่ได้/บัคระบบ
- ถามหมายเลขหวยเด็ด/ทำนายเลข/ฟันธงเลข
- ถามเงื่อนไขโปรที่ซับซ้อน/โบนัสพิเศษเฉพาะราย
- คำถามที่คุณไม่แน่ใจ 100%

## กฎการตอบ (รีเซฟชั่นมืออาชีพ):
1. **ห้ามสร้างข้อมูลเอง** - ตอบเฉพาะที่รู้แน่นอน
2. **ห้ามระบุตัวเลขแน่นอน** เกี่ยวกับเงิน/เวลา/เปอร์เซ็นต์ ถ้าไม่แน่ใจ
3. **ถ้าไม่แน่ใจ 100%** → บอกว่า "ดิฉันจะประสานงานให้แอดมินตรวจสอบค่ะ"
4. **ห้ามคุยเรื่องดวง/ทำนาย/เลขเด็ด** → บอกว่าไม่สามารถให้คำแนะนำได้
5. **Tone รีเซฟชั่น**: อบอุ่น ช่วยเหลือ มั่นใจ "รับเรื่องค่ะ" "จะประสานงานให้"
6. ลิงก์สมัคร: ${signupUrl}
7. ติดต่อแอดมิน: ${handle}

## ตัวอย่างการตอบแบบรีเซฟชั่น:
- "น้องมีโชครับเรื่องค่ะ จะประสานงานให้แอดมินทันที"
- "ดิฉันจะช่วยประสานงานให้ค่ะ"
- "น้องมีโชคจะส่งต่อให้แอดมินดูแลให้โดยเร็วค่ะ"
  `.trim();

  const userPrompt = `
ลูกค้าถาม: "${userText}"

ให้ตอบในรูปแบบ JSON เท่านั้น:
{
  "shouldEscalate": true/false,  // true = ต้องส่งต่อแอดมิน, false = ตอบเองได้
  "confidence": "high/medium/low",  // ความมั่นใจในคำตอบ
  "response": "คำตอบ (เริ่มด้วย 'ผู้ช่วย AI...' หรือคล้ายๆ)"
}

เกณฑ์ shouldEscalate = true:
- คำถามเกี่ยวกับเงิน/ยอดเครดิต/การโอนเงิน ของบุคคลนั้นเฉพาะเจาะจง
- ต้องการข้อมูลส่วนตัว (ยูส/รหัสผ่าน)
- ปัญหาเทคนิค (ล็อกอินไม่ได้/บัค)
- ถามเลขเด็ด/ทำนาย
- คำถามที่คุณไม่มั่นใจ 100%
  `.trim();

  try {
    const resp = await openai.chat.completions.create({
      model: "gpt-4o-mini",
      temperature: 0.2, // ลดจาก 0.7 → 0.2 เพื่อให้คำตอบสม่ำเสมอ ไม่สร้างสรรค์เกินไป
      response_format: { type: "json_object" },
      messages: [
        { role: "system", content: systemPrompt },
        { role: "user", content: userPrompt },
      ],
    });

    const content = resp.choices?.[0]?.message?.content?.trim() || "{}";
    const parsed = JSON.parse(content);

    return {
      response: parsed.response || "ขออภัยค่ะ ระบบขัดข้องชั่วคราว 🙏",
      shouldEscalate: parsed.shouldEscalate === true,
      confidence: parsed.confidence || "low",
    };
  } catch (e) {
    console.error("[askAI error]", e);
    // ถ้า error ให้ส่งต่อแอดมินเสมอเพื่อความปลอดภัย
    return {
      response: "ขออภัยค่ะ ระบบขัดข้องชั่วคราว กรุณาติดต่อแอดมินเพื่อความรวดเร็วนะคะ 🙏",
      shouldEscalate: true,
      confidence: "low",
    };
  }
}

/**
 * Legacy function สำหรับ backward compatibility
 * ใช้ askAI แล้วคืนแค่ string
 */
export async function askAILegacy(userText: string, ctx: AskAIContext = {}): Promise<string> {
  const result = await askAI(userText, ctx);
  return result.response;
}

/** ใช้ใน /api/line/push (ถ้ามี) เพื่อดึงข่าวเลขเด็ด/ดวงจาก endpoint ภายในโปรเจกต์ */
export async function fetchLuckyNews(limit = 5): Promise<LuckyItem[]> {
  const internalOrigin =
    process.env.LUCKY_INTERNAL_ORIGIN ||
    (process.env.VERCEL_URL ? `https://${process.env.VERCEL_URL}` : "");

  const endpoints = [internalOrigin ? `${internalOrigin}/api/lucky-feed` : ""].filter(Boolean) as string[];

  for (const ep of endpoints) {
    try {
      const res = await fetch(ep, { method: "GET", cache: "no-store" });
      if (!res.ok) continue;
      const data = await res.json();
      const items: LuckyItem[] = Array.isArray(data?.items)
        ? data.items.map((it: any) => ({
            title: String(it.title || ""),
            url: String(it.url || it.link || ""),
            imageUrl: it.imageUrl ? String(it.imageUrl) : undefined,
            source: it.source ? String(it.source) : undefined,
            publishedAt: it.publishedAt ? String(it.publishedAt) : undefined,
          }))
        : [];
      if (items.length) return items.slice(0, limit);
    } catch {}
  }
  return [];
}