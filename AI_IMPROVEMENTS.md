# 🤖 การปรับปรุงระบบ AI Chatbot

## 🎯 เป้าหมาย
แก้ไขปัญหาบอทตอบไม่ตรงจุด ตอบมั่ว ทำให้ลูกค้าหัวร้อน → **ให้ตอบแม่นยำและระมัดระวังมากขึ้น**

---

## ✅ การปรับปรุงที่ทำไปแล้ว

### 1. ⚙️ ปรับ AI System Prompt (lib/ai.ts)

**เปลี่ยนจาก:**
- Temperature = 0.7 (สูง → ตอบสร้างสรรค์มั่ว)
- บอกให้ตอบ "ทุกเรื่อง" รวมดวง/ความฝัน/เลขเด็ด
- ไม่มีกฎห้ามชัดเจน

**เปลี่ยนเป็น:**
- ✅ Temperature = 0.2 (ตอบสม่ำเสมอ ไม่สร้างสรรค์เกินไป)
- ✅ ห้ามตอบเรื่อง: เงิน/รหัสผ่าน/เลขเด็ด/ดวง/ปัญหาเทคนิค
- ✅ ใช้ JSON response format พร้อม `shouldEscalate` flag
- ✅ ต้อง "ยอมรับว่าไม่รู้" และส่งต่อแอดมิน

**ตัวอย่าง Prompt:**
```
## ⛔ ข้อห้าม - ส่งต่อแอดมินทันทีถ้า:
- ถามเรื่องยูสเซอร์/รหัสผ่าน/ข้อมูลส่วนตัว
- ถามเรื่องเงินเข้า-ออก ของบุคคลเฉพาะ
- ถามปัญหาทางเทคนิค/ล็อกอินไม่ได้
- ถามหมายเลขหวยเด็ด/ทำนายเลข
- คำถามที่คุณไม่แน่ใจ 100%

## กฎการตอบ:
1. **ห้ามสร้างข้อมูลเอง** - ตอบเฉพาะที่รู้แน่นอน
2. **ห้ามระบุตัวเลขแน่นอน** เกี่ยวกับเงิน/เวลา
3. **ถ้าไม่แน่ใจ 100%** → ส่งต่อแอดมิน
```

---

### 2. 🎯 เพิ่ม Confidence Scoring & Escalation

**ระบบใหม่ (AskAIResult):**
```typescript
{
  response: string,           // คำตอบ
  shouldEscalate: boolean,   // ควรส่งต่อแอดมินหรือไม่
  confidence: "high" | "medium" | "low"  // ความมั่นใจ
}
```

**Flow การตอบ:**
1. AI ประเมินคำถาม
2. ถ้า `shouldEscalate = true` หรือ `confidence = low` → แนะนำติดต่อแอดมิน
3. ถ้า `confidence = high/medium` → ตอบตามปกติ

**ข้อความที่แสดงเมื่อส่งต่อ:**
```
[คำตอบเบื้องต้นจาก AI]

📞 เรื่องนี้แอดมินจะดูแลให้ดีกว่านะคะ
กรุณาติดต่อ: @mechoke เพื่อความรวดเร็วค่ะ
```

---

### 3. 🛡️ Response Validation Layer (lib/response-validator.ts)

**ตรวจสอบคำตอบก่อนส่ง:**
- ❌ ระบุจำนวนเงินแน่นอน (เช่น "ได้เงิน 5000 บาท")
- ❌ ให้ username/password
- ❌ บอกเบอร์โทรศัพท์ 10 หลัก
- ❌ แนะนำเลขเด็ด/ทำนาย
- ❌ ระบุเปอร์เซ็นต์แน่นอนโดยไม่มีคำว่า "ประมาณ"
- ❌ คำตอบสั้นเกิน (<10 ตัวอักษร)
- ❌ คำตอบยาวเกิน (>800 ตัวอักษร)

**ถ้า validation ไม่ผ่าน:**
```
ขออภัยค่ะ เรื่องนี้ซับซ้อนเล็กน้อย
แอดมินจะตรวจสอบและตอบให้ถูกต้องแน่นอนค่ะ 💚

📞 กรุณาติดต่อ: @mechoke
```

---

### 4. 📚 ขยาย Intent-Based Router

**เพิ่ม Intent Patterns ใหม่:**
- ความปลอดภัย/โกง/ปลอม → ตอบเรื่องความน่าเชื่อถือ
- ขอคืนเงิน/ยกเลิก → อธิบายเงื่อนไข
- แจ้งปัญหาเร่งด่วน → แนะนำติดต่อแอดมินพร้อมข้อมูล
- ขอเลขเด็ด/ทำนาย → บอกว่าไม่สามารถแนะนำได้

**ผลลัพธ์:**
- ลดโอกาสที่ต้องไป fallback AI
- คำตอบเป็นสคริปต์สำเร็จรูป (ไม่มั่ว)

---

## 📊 การเปรียบเทียบ

| ด้าน | ก่อนปรับปรุง | หลังปรับปรุง |
|------|------------|-------------|
| **Temperature** | 0.7 (สูง) | 0.2 (ต่ำ) |
| **ห้ามตอบเรื่องละเอียดอ่อน** | ไม่มี | มี ✅ |
| **Confidence check** | ไม่มี | มี ✅ |
| **Escalation to human** | ไม่มี | มี ✅ |
| **Response validation** | ไม่มี | มี ✅ |
| **Intent coverage** | ~15 กรณี | ~24 กรณี |
| **ตอบมั่ว** | บ่อย ❌ | ลดลงมาก ✅ |

---

## 🧪 วิธีทดสอบ

### Test Case 1: คำถามเกี่ยวกับเงิน
```
ลูกค้า: "เครดิตผมหายไป 5000 บาท"
ก่อน: AI อาจตอบมั่ว "ระบบตรวจสอบแล้วค่ะ..."
ตอนนี้: → ส่งต่อแอดมิน ✅
```

### Test Case 2: ขอเลขเด็ด
```
ลูกค้า: "แนะนำเลขเด็ดงวดนี้หน่อย"
ก่อน: AI อาจตอบแนะนำเลข
ตอนนี้: → Router จับได้ ตอบว่า "ไม่สามารถแนะนำได้" ✅
```

### Test Case 3: คำถามทั่วไป
```
ลูกค้า: "ฝากเงินยังไง"
ก่อน: AI ตอบ (อาจผิด)
ตอนนี้: → Router จับได้ ตอบด้วยสคริปต์สำเร็จรูป ✅
```

### Test Case 4: คำถามที่ไม่แน่ใจ
```
ลูกค้า: "ทำไมหวยฮานอยออกผลช้า"
ก่อน: AI ตอบสมมติ (อาจผิด)
ตอนนี้: → AI ประเมินว่า confidence = low → ส่งต่อแอดมิน ✅
```

---

## 📈 ผลที่คาดหวัง

### ✅ ลดปัญหา:
1. **ตอบมั่วเรื่องเงิน** → เกือบ 0%
2. **แนะนำเลขเด็ด** → 0% (ถูก block ทั้ง Router + AI)
3. **ข้อมูลผิดพลาด** → ลด 80-90%
4. **ลูกค้าหัวร้อน** → ลด 70%+

### ✅ เพิ่มความปลอดภัย:
- ไม่ให้ username/password/เบอร์โทร
- ไม่ระบุตัวเลขเงินแน่นอน
- ส่งต่อแอดมินเมื่อไม่แน่ใจ

---

## 🔧 การตั้งค่าเพิ่มเติม

### 1. ตรวจสอบ Logs
```bash
# ดู AI decisions
grep "\[ai-decision\]" logs

# ดู validation failures
grep "\[ai-validation-failed\]" logs

# ดู escalations
grep "shouldEscalate" logs
```

### 2. ปรับแต่ง Intent Router
เพิ่ม patterns ใน `app/api/line/webhook/route.ts` ฟังก์ชัน `routeQuickAnswerToMessages()`

### 3. ปรับแต่ง Validation Rules
แก้ไขใน `lib/response-validator.ts` ตามความต้องการ

---

## ⚠️ สิ่งที่ต้องระวัง

1. **Over-escalation**: ถ้าส่งต่อแอดมินบ่อยเกิน → ลด sensitivity ของ validation
2. **Under-escalation**: ถ้า AI ยังตอบผิด → เพิ่ม rules ใน validator
3. **Intent coverage**: ถ้าเจอคำถามใหม่บ่อย → เพิ่ม intent pattern

---

## 📞 หากพบปัญหา

1. เช็ค logs: `[ai-decision]` และ `[ai-validation-failed]`
2. ทดสอบคำถามที่มีปัญหาซ้ำ
3. ปรับแต่ง system prompt หรือ validation rules
4. ติดต่อทีม dev พร้อม logs

---

## 📚 ไฟล์ที่เกี่ยวข้อง

- `lib/ai.ts` - AI system prompt & temperature
- `lib/response-validator.ts` - Response validation
- `app/api/line/webhook/route.ts` - Intent router & webhook handler
- `AI_IMPROVEMENTS.md` - เอกสารนี้

---

**สรุป:** ระบบใหม่ปลอดภัยและระมัดระวังมากขึ้น ลด hallucination และรู้จัก "ยอมรับว่าไม่รู้" แล้วส่งต่อแอดมิน 🎉
